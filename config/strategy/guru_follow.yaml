# Strategy: guru + optional token filter + sizing.

guru_wallet_address: "0x204f72f35326db932158cba6adff0b9a1da95e14"

# Explicit switch: false = follow all guru tokens (risk/execution still apply).
# true = only allowlisted_token_ids (production-style).
token_filter:
  enabled: false
  allowlisted_token_ids: []

copy_scale: 1.0
# strategy_dedup_state_path: "var/guru_dedup_strategy.json"
