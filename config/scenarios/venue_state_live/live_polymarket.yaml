# =============================================================================
# Runtime — VenueState live validation (post–Step 5 migration).
#
# Tier A (caps, collateral snapshot, open orders, layer A long qty, bot-sell
# inventory) reads from VenueState when live + wallet_sync_enabled — no feature
# flag, no position_reconciliation pass.
#
# Replace trader_id / paths if you shard multiple test runs.
# =============================================================================

trader_id: "TYREX-VENUE-STATE-LIVE-001"
execution_mode: live

guru_poll_interval_seconds: 30.0
data_api_base_url: "https://data-api.polymarket.com"
guru_state_path: "var/scenarios/venue_state_live/guru_watermark.json"
guru_dedup_state_path: "var/scenarios/venue_state_live/guru_dedup.json"
guru_activity_limit: 200
guru_max_activity_pages_per_poll: 4
guru_startup_backfill_seconds: 0.0

guru_ingest_mode: rtds_primary
guru_ingest_phase: "venue_state_live"
guru_rtds_url: "wss://ws-live-data.polymarket.com"
guru_rtds_liveness_timeout_seconds: 120.0
guru_rtds_reconnect_retry_initial_seconds: 1.0
guru_rtds_reconnect_retry_max_seconds: 60.0
guru_rtds_ping_interval_seconds: 5.0
guru_poll_fallback_enabled: true
guru_poll_fallback_interval_seconds: null
guru_gap_fill_enabled: true
guru_gap_fill_lookback_seconds: 60.0
guru_proxy_wallet_validation_required: false
guru_stream_queue_drain_interval_ms: 50

logging_level: INFO
clob_host: "https://clob.polymarket.com"
chain_id: 137

polymarket_instrument_ids: []
polymarket_dynamic_instruments: true
polymarket_dynamic_max_activations: 32
polymarket_gamma_base_url: "https://gamma-api.polymarket.com"
polymarket_gamma_http_timeout_seconds: 15.0
polymarket_startup_token_warmup_max: 32

execution_entry_guard_enabled: false
execution_max_entry_slippage_ticks: 0
execution_book_depth_clip_enabled: false
execution_book_depth_utilization_cap: 1.0
execution_book_rest_snapshot_enabled: false
execution_book_strict: false
execution_limit_timeout_enabled: false
execution_limit_timeout_seconds: 30.0

# --- Wallet sync → feeds VenueState (same HTTP; no duplicate bulk fetch) -----
wallet_sync_enabled: true
wallet_sync_poll_interval_seconds: 15.0
wallet_sync_startup_deadline_seconds: 120.0
wallet_sync_per_instrument_max_retries: 3

polymarket_use_data_api_for_positions: true

# --- VenueState (Tier A snapshot TTL / CLOB cash / refresh cap) --------------
# Defaults match RuntimeSettings; set explicitly here for a documented test run.
venue_state_ttl_seconds: 30.0
venue_state_cash_poll_interval_seconds: 10.0
venue_state_refresh_force_max_ms: 500

# --- Nautilus exec engine continuous checks (unchanged; not “reconciliation”) -
exec_position_check_interval_seconds: 45
exec_open_check_interval_seconds: 20

# --- Startup / shutdown -------------------------------------------------------
startup_readiness_timeout_seconds: 120.0
shutdown_drain_enabled: true
shutdown_drain_timeout_seconds: 30.0

reporting_enabled: true
reporting_base_dir: "var/reporting/runs"
reporting_sink_max_queue: 50000
reporting_sink_batch_size: 128
reporting_capital_observability_enabled: true
reporting_capital_snapshot_period_seconds: 300.0
