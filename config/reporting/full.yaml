schema_version: 1
profile: full

# Mandatory evidence cannot be disabled. Do not set outputs.manifest,
# outputs.run_summary, outputs.audit_events, or channels.operational to false.

analytics:
  per_evaluation: true
  indicators: true
  signals: true
  debug_host_trace: false
  closest_candidates_per_reason: 5

raw_recording:
  enabled: false
