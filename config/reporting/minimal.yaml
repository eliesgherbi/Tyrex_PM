schema_version: 1
profile: minimal

# Mandatory evidence cannot be disabled. Aggregate counters remain complete
# even when optional per-evaluation analytics are reduced.

analytics:
  per_evaluation: false
  indicators: false
  signals: false
  debug_host_trace: false
  closest_candidates_per_reason: 5

raw_recording:
  enabled: false
