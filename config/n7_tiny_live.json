{
  "mode": "n7_oneshot",
  "one_shot": true,
  "market_family": "btc_updown_5m",
  "max_buy_collateral": "5.00",
  "max_daily_notional": "5.00",
  "max_daily_loss": "5.00",
  "max_positions_per_window": 1,
  "max_entry_lineages": 1,
  "allow_same_window_reentry": false,
  "allow_same_window_reversal": false,
  "skip_if_min_exceeds_cap": true,
  "order_style": "marketable_limit",
  "require_ssr_price_match": false,
  "live": {
    "enabled": false,
    "mutations_enabled": false,
    "scope": "A",
    "ack_timeout_ms": 15000,
    "max_order_notional": "5.00",
    "hard_collateral_cap": "5.00",
    "order_style": "marketable_limit"
  },
  "timing": {
    "last_allowed_entry_before_end_s": 180.0,
    "discretionary_exit_cutoff_before_end_s": 120.0,
    "mandatory_flatten_start_before_end_s": 90.0,
    "residual_operator_deadline_before_end_s": 45.0,
    "event_end_safety_buffer_s": 30.0,
    "acknowledgment_timeout_s": 15.0,
    "cancel_recon_budget_s": 10.0,
    "exit_retry_max_attempts": 3,
    "exit_retry_time_budget_ms": 60000,
    "ack_timeout_ms": 15000,
    "status": "FROZEN_FOR_N7",
    "justification": {
      "boundary_lag_s": "1.6-5.5 (N1) → ack 15s",
      "cl_bn_skew_p95_s": "2.6 (N1) → ≥5s flatten slack",
      "n5_flatten_before_end_s": "20 → safety buffer 30s",
      "prep_lead_s": "≥30-60 recommended → last entry 180s before end on 300s window"
    }
  },
  "z_gap": {
    "resolution_capability": false,
    "target_notional": "5",
    "fee_rate": "0.07",
    "fee_exponent": "1"
  },
  "notes": {
    "daily_notional_accounting": "entry exposure only; inventory-reducing exits do not add notional",
    "fee_inclusive_entry_cap": "worst_price * qty + conservative_entry_fee <= 5.00; size downward; never raise cap",
    "exit_fees": "may reduce proceeds; never block inventory-reducing exits",
    "ptb_authority": "chainlink_sealed_k when require_ssr_price_match=false; SSR openPrice match is optional/opt-in",
    "authorization": "operator runs: python tools/n7_live/run_n7_live_oneshot.py --live (no phrase/envelope)"
  }
}
