# Copy to `.env` in the repository root. Never commit `.env`.
#
# New Tyrex_PM reset skeleton (R1): no venue credentials required yet.
# Add Polymarket / Binance variables in later phases when adapters consume them.

# Optional: load path overrides for local tooling (unused by R1 CLI).
# TYREX_HOME=
