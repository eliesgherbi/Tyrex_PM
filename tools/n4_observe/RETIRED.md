# Retired

Standalone N4 observe tools were retired in the reporting-system cutover (M7).

Use:

- `tyrex-pm run --mode observe ...` for OBSERVE
- `N4ObserveRuntime` remains in `src/tyrex_pm/runtime/n4_observe_runtime.py` for N7 composition
